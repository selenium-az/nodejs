##
# Local path of the JSON file containing test data (mandatory).
# Local path can be relative to the current directory or to the root directory. 
SELENIUM_TEST_DATA=.\\test_data\\test_suite.json

##
# Coded name of browser to use (mandatory).
# Allowed names are <firefox>, <chrome> and <ie>.
SELENIUM_BROWSER=firefox

##
# User agent to use with tests (mandatory).
SELENIUM_BROWSER_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64; rv:66.0) Gecko/20100101 Firefox/66.0

##
# Base URL of web site.
# When set overrides the base URL of test suite.
#SELENIUM_TARGET_URL=https://stage.alitalia.com/it_it/

##
# Flag to use the browser headless mode (no user interface).
# This flag (optional) has no effect with IE and Edge browsers.
# Allowed values are 'true' (default) and 'false'.
#SELENIUM_HEADLESS=true

##
# Setting (optional) used for initial browser window size.
# Values must be formatted as '<WIDTH>x<HEIGHT>' (default is '1024x768').
#SELENIUM_WINDOW_SIZE=1024x768

##
# Setting (optional) used to change logging level during execution.
# Allowed values are 'INFO' (default) or 'DEBUG'.
#SELENIUM_LOG_LEVEL=INFO

##
# Setting (optional) used to change command execution timeout interval.
# Allowed values are positive integers (default is 60s)
#SELENIUM_COMMAND_TIMEOUT=60
